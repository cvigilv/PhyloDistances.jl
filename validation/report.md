# Agreement with TreeDist

Generated by `julia --project=validation validation/crosscheck.jl`.

Integers are compared exactly and floating-point values bitwise — no 
tolerance anywhere. Cases cover trees with no splits at all, maximal 
imbalance, polytomies on one and both sides, rooted against unrooted, and 
identical against maximally different, alongside random trees.

- Julia 1.12.6, TreeDist 2.14.1, ape 5.8.1, R 4.4.2
- Cases compared: 2140

| quantity | comparison | mismatches |
|---|---|---:|
| `RobinsonFoulds()` | exact integer | 0 |
| `RobinsonFoulds(normalize = true)` | bitwise float | 0 |

`NaN` agreed with `NaN` in 16 cases, where neither tree carries a split and the normalizer is zero.

Every value is identical.
